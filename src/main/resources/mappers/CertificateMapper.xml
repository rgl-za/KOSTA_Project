<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.CertificateMapper">

	<sql id="certificateColumns">
		cernum
		, pnum
		, cfile
		, regdate
		, userid
		, title
		, content
		, deleteyn
		, deletetime
	</sql>
	
	<!-- 구매한 상품 인증 작성 --> <!-- 테스트 성공 -->
	<insert id="insertCer" parameterType="CertificateDTO">
		INSERT INTO certificate (
			<include refid="certificateColumns"/>
		) VALUES (
			seq_cernum_no.nextval
			, 61			<!-- #{pnum} -->
			, #{cfile}
			, sysdate
			, 'munwjdgus1'	<!-- #{userid} -->
			, #{title}
			, #{content}
			, 'N'			<!-- #{deleteyn} -->
			, NULL			<!-- #{deletetime} -->
		)
	</insert>
	
	<!-- 글 상세 내용 -->
	<select id="selectCerDetail" parameterType="CertificateDTO" resultType="CertificateDTO">
		SELECT
			<include refid="certificateColumns"/>
		FROM
			certificate
		WHERE
			deleteyn = 'N'
		AND
			cernum = #{cernum}
	</select>
	
	<!-- 수정 -->
	<update id="updateCer" parameterType="CertificateDTO">
		UPDATE post
		SET
			regdate 	= SYSDATE
			, cfile 	= #{cfile}
			, title 	= #{title}
			, content 	= #{content}
		WHERE
			cernum = #{cernum}
	</update>
	
	<!-- 삭제 -->
	<update id="deleteCer" parameterType="long">
		UPDATE certificate
		SET
			deleteyn = 'Y'
			, deletetime = SYSDATE 	<!-- delete_time 컬럼 필요 -->
		WHERE
		cernum = #{cernum}
	</update>
	
	<!-- /인증서 작성 -->
	
	
	<!-- 인증서 목록 --> <!-- 테스트 성공 -->
	<select id="getCerList" parameterType="CertificateDTO" resultType="CertificateDTO">
		SELECT
			<include refid="certificateColumns"/>
		FROM 
			certificate
		WHERE
			deleteyn = 'N'
		ORDER BY regdate DESC
	</select>
	
	<insert id="insertCerFileList" parameterType="CerFileDTO">
			INSERT INTO t_file
			(
				cernum,
				originalfilename,
				storedfilepath,
				filesize,
				creatorid,
				createddatetime
			)
			VALUES
		<foreach collection="list" item="item" separator=",">
			(
				#{item.cernum},
				#{item.originalFileName},
				#{item.storedFilePath},
				#{item.fileSize},
				'admin',
				SYSDATE
			)
		</foreach>
	</insert>
	
	<select id="selectCerFileList" parameterType="int" resultType="CerDTO">
			SELECT
				fidx,
				cernum,
				originalfilename,
				FORMAT(ROUND(file_size / 1024), 0) AS filesize
			FROM
				t_file
			WHERE
				cernum = #{cernum}
				AND deletedyn = 'N'
	</select>
	
	<select id="selectCerFileInformation" parameterType="map" resultType="CerDTO">
			SELECT
				originalfilename,
				storedfilepath,
				filesize
			FROM
				t_file
			WHERE
				fidx = #{fidx}
				AND cernum = #{cernum}
				AND deletedyn = 'N'
	</select>
	
	<insert id="insertCerFileList" parameterType="CerFileDTO">
			INSERT INTO t_file
			(
				boardIdx,
				originalFileName,
				storedFilePath,
				fileSize,
				creatorid,
				createddatetime
			)
			VALUES
		<foreach collection="list" item="item" separator=",">
			(
				#{item.SEQ_BOARDIDX_NO.nextval},
				#{item.originalFileName},
				#{item.storedFilePath},
				#{item.fileSize},
				'admin',
				SYSDATE
			)
		</foreach>
	</insert>
	
	<select id="selectCerFileList" parameterType="int" resultType="CerFileDTO">
			SELECT
				idx
				,boardIdx
				,originalfilename
				,FORMAT(ROUND(filesize / 1024), 0) AS filesize
			FROM
				t_file
			WHERE
				boardIdx = #{boardIdx}
				AND deletedyn = 'N'
	</select>
	
	<select id="selectCerFileInformation" parameterType="map" resultType="CerFileDTO">
			SELECT
				originalfilename
				,storedfilepath
				,filesize
			FROM
				t_file
			WHERE
				idx = #{idx}
				AND boardidx = #{boardIdx}
				AND deletedyn = 'N'
	</select>
	
</mapper>