<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.postMapper.Project.mapper">

	<sql id="postColumns">
		pnum
		, catnum
		, leaderid
		, title
		, content
		, photo
		, link
		, price
		, postnum
		, dealaddress
		, maxpeople
		, minpeople
		, accountpost
		, uploaddate
		, enddate
		, delete_yn
	</sql>
	
	<!-- 수정 필요 (문정현) -->
	<insert id="insertPost" parameterType="PostDTO">
<<<<<<< HEAD
		INSERT INTO post (
		<include refid="postColumns" />
		) VALUES (
		pnum.nextval
		, #{catnum}
		, #{leaderid}
		, #{title}
		, #{content}
		, #{photo}
		, #{link}
		, #{price}
		, #{postnum}
		, #{dealaddress}
		, #{maxpeople}
		, #{minpeople}
		, #{accountpost}
		, sysdate 			<!-- #{uploaddate} -->
		, #{enddate}
		, #{delete_yn}
		)
=======
			INSERT INTO post (
			<include refid="postColumns" />
			) VALUES (
			seq_post_no.nextval
			, 1
			, 'NULL'
			, #{title}
			, #{content}
			, #{photo}
			, #{link}
			, #{price}
			, #{postnum}
			, 'NULL'
			, #{maxpeople}
			, #{minpeople}
			, 'NULL'
			, SYSDATE 			
			, #{enddate}
			, 'N'
			, #{finaldate}		
			)
>>>>>>> main
	</insert>
	
	<!-- 수정 필요 (문정현)-->
	<update id="updatePost" parameterType="PostDTO">
		UPDATE post
		SET
		uploaddate 	= SYSDATE
		, title 	= #{title}
		, photo 	= #{photo}
		, link 		= #{link}
		, content 	= #{content}
		, price 	= #{price}
		, maxpeople = #{maxpeople}
		, minpeople = #{minpeople} <!-- 고정값 1로 -->
		, enddate 	= #{enddate}
		WHERE
		pnum = #{pnum}
	</update>


	<!-- 남혁이형 부분 -->
	<update id="deletePost" parameterType="long">
		UPDATE post
		SET
		delete_yn = 'Y'
		, delete_time = SYSDATE 	<!-- delete_time 컬럼 필요  -->
		WHERE
		pnum = #{pnum}
	</update>
	



	<select id="getPost" parameterType="int" resultType="PostDTO">
		SELECT
			*
		FROM
			post
		WHERE pnum = #{pnum}
	</select>

</mapper>